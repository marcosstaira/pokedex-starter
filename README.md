🛠️ Pré-requisitos

Antes de começar, certifique-se de ter o ambiente configurado:

Node.js (versão LTS recomendada).

Gerenciador de pacotes (npm ou yarn).

Expo Go instalado no seu celular.

📦 Instalação

Clone o repositório:

git clone https://github.com/marcosstaira/pokedex-starter.git cd pokedex-starter

Instale as dependências:

npm install

ou

yarn install

Isso instalará bibliotecas essenciais como expo-router, @react-native-async-storage/async-storage, @react-native-community/netinfo, etc.

▶️ Como Executar

Para iniciar o servidor de desenvolvimento (Metro Bundler):

npx expo start

Opções de Visualização:

No Celular físico:

Abra o app Expo Go.

Escaneie o QR Code exibido no terminal (tem que estar na mesma rede, caso não esteja utilize ao final do comando no terminal --tunnel)

Na Web:

Pressione w para abrir no navegador.
